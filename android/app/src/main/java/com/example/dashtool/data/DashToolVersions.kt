package com.example.dashtool.data

/*
 * Increment these values only when the corresponding
 * logic changes in a way that affects saved data.
 */
object DashToolVersions {

    /*
     * Version 1 is the current rating formula.
     */
    const val ENGINE_VERSION =
        1

    /*
     * Version 2 includes the improved restaurant
     * parser that requires both payout and mileage.
     */
    const val PARSER_VERSION =
        2
}
